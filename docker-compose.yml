services:
  react-dev:
    build:
      context: ./react-app
      dockerfile: Dockerfile.dev
    volumes:
      - ./react-app:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true
    restart: unless-stopped
